# Ablation: reduce num_modes to study effect on expressibility vs the default
# num_modes=8 in defaults.yaml. Uses the same pure-PyTorch Fock engine.

name: cv_quixer_fewer_modes
model: quantum

quantum:
  num_modes: 4
  num_layers: 4
  cutoff_dim: 10
